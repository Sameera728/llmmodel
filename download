import React, { useEffect, useState } from "react";
import { Cpu, Database, HardDrive, ShieldCheck, Activity, Terminal, AlertCircle, FileCode, CheckCircle2 } from "lucide-react";

export default function MetricsDashboard() {
  const [stats, setStats] = useState<any>({
    active_model: "qwen-2.5",
    gpu_simulated: "NVIDIA RTX 4090",
    cuda_version: "12.1",
    status: "healthy"
  });

  useEffect(() => {
    fetch("/api/health")
      .then((res) => res.json())
      .then((data) => setStats(data))
      .catch((e) => console.error(e));
  }, []);

  return (
    <div className="space-y-6">
      {/* Bento Grid Metrics */}
      <div className="grid grid-cols-1 md:grid-cols-4 gap-4">
        {/* Metric 1 */}
        <div className="bg-zinc-900/40 backdrop-blur-md border border-zinc-800 rounded-xl p-4 flex items-center gap-4 shadow-md">
          <div className="w-10 h-10 rounded-lg bg-blue-600/10 border border-blue-500/20 flex items-center justify-center shrink-0">
            <Cpu className="text-blue-500" size={18} />
          </div>
          <div className="min-w-0">
            <span className="text-[10px] text-zinc-500 font-mono block uppercase">Active Cores</span>
            <span className="text-sm font-bold text-zinc-100 font-mono truncate block">{stats.active_model.toUpperCase()}</span>
          </div>
        </div>

        {/* Metric 2 */}
        <div className="bg-zinc-900/40 backdrop-blur-md border border-zinc-800 rounded-xl p-4 flex items-center gap-4 shadow-md">
          <div className="w-10 h-10 rounded-lg bg-emerald-500/10 border border-emerald-500/20 flex items-center justify-center shrink-0">
            <Activity className="text-emerald-400" size={18} />
          </div>
          <div className="min-w-0">
            <span className="text-[10px] text-zinc-500 font-mono block uppercase">GPU Acceleration</span>
            <span className="text-sm font-bold text-zinc-100 font-mono truncate block">{stats.gpu_simulated}</span>
          </div>
        </div>

        {/* Metric 3 */}
        <div className="bg-zinc-900/40 backdrop-blur-md border border-zinc-800 rounded-xl p-4 flex items-center gap-4 shadow-md">
          <div className="w-10 h-10 rounded-lg bg-blue-500/10 border border-blue-500/20 flex items-center justify-center shrink-0">
            <Database className="text-blue-400" size={18} />
          </div>
          <div className="min-w-0">
            <span className="text-[10px] text-zinc-500 font-mono block uppercase">Local Database</span>
            <span className="text-sm font-bold text-zinc-100 font-mono truncate block">SQLite (Dev Ready)</span>
          </div>
        </div>

        {/* Metric 4 */}
        <div className="bg-zinc-900/40 backdrop-blur-md border border-zinc-800 rounded-xl p-4 flex items-center gap-4 shadow-md">
          <div className="w-10 h-10 rounded-lg bg-purple-500/10 border border-purple-500/20 flex items-center justify-center shrink-0">
            <ShieldCheck className="text-purple-400" size={18} />
          </div>
          <div className="min-w-0">
            <span className="text-[10px] text-zinc-500 font-mono block uppercase">Runtime Security</span>
            <span className="text-sm font-bold text-zinc-100 font-mono truncate block">FastAPI + JWT Auth</span>
          </div>
        </div>
      </div>

      {/* Main Panel */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6 items-stretch">
        {/* Core Integration Status */}
        <div className="lg:col-span-1 bg-zinc-900/40 backdrop-blur-md rounded-xl border border-zinc-800 p-5 flex flex-col justify-between">
          <div>
            <div className="flex items-center gap-2 mb-3 pb-3 border-b border-zinc-800">
              <Terminal size={15} className="text-blue-500" />
              <h3 className="text-xs font-semibold text-zinc-400 uppercase tracking-wider">
                System Integration
              </h3>
            </div>

            <div className="space-y-3 font-mono text-[11px] text-zinc-400">
              <div className="flex justify-between border-b border-zinc-800/40 pb-1.5">
                <span>CUDA toolkit:</span>
                <span className="text-zinc-200">v{stats.cuda_version || "12.1"}</span>
              </div>
              <div className="flex justify-between border-b border-zinc-800/40 pb-1.5">
                <span>NVIDIA Drivers:</span>
                <span className="text-zinc-200">{stats.driver_version || "535.113.01"}</span>
              </div>
              <div className="flex justify-between border-b border-zinc-800/40 pb-1.5">
                <span>Inference precision:</span>
                <span className="text-blue-400">float16 / bfloat16</span>
              </div>
              <div className="flex justify-between border-b border-zinc-800/40 pb-1.5">
                <span>Authentication:</span>
                <span className="text-emerald-400">OAuth / SQLite API</span>
              </div>
              <div className="flex justify-between">
                <span>Deployment schema:</span>
                <span className="text-blue-400">Docker Compose / Compose v2</span>
              </div>
            </div>
          </div>

          <div className="mt-6 p-3 bg-emerald-500/5 rounded-lg border border-emerald-500/10 flex gap-2 items-start">
            <CheckCircle2 size={15} className="text-emerald-400 shrink-0 mt-0.5" />
            <div className="text-[10px] text-zinc-400 leading-relaxed font-mono">
              <span className="font-semibold text-zinc-200 block">FastAPI Server Standby</span>
              Uvicorn ASGI runner mapped to port 8000. All modules (RAG context selectors, model controllers, trainer threads) are fully operational.
            </div>
          </div>
        </div>

        {/* Codebase architecture map */}
        <div className="lg:col-span-2 bg-zinc-900/40 backdrop-blur-md rounded-xl border border-zinc-800 p-5 flex flex-col h-full">
          <div className="flex items-center gap-2 mb-3 pb-3 border-b border-zinc-800">
            <FileCode size={15} className="text-blue-500" />
            <h3 className="text-xs font-semibold text-zinc-400 uppercase tracking-wider">
              GPT-Style Modular Architecture Overview
            </h3>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-3 flex-1 overflow-y-auto custom-scrollbar">
            {/* Box 1 */}
            <div className="bg-[#09090b]/45 border border-zinc-800/80 rounded-lg p-3 space-y-1">
              <span className="text-xs font-bold text-blue-400 font-mono">configs/config.yaml</span>
              <p className="text-[10px] text-zinc-400 leading-relaxed">
                Central configuration store hosting parameter switches for active models (LLaMA-3, Mistral, Qwen, Gemma, Phi), LoRA ranks, dataset batch sizes, and FAISS RAG weights.
              </p>
            </div>

            {/* Box 2 */}
            <div className="bg-[#09090b]/45 border border-zinc-800/80 rounded-lg p-3 space-y-1">
              <span className="text-xs font-bold text-blue-400 font-mono">backend/main.py</span>
              <p className="text-[10px] text-zinc-400 leading-relaxed">
                FastAPI web gateway exposing modular REST endpoints for chat completions, dynamic hot-swapping baseline weights, indexing ingested PDFs, and launching fine-tuning jobs.
              </p>
            </div>

            {/* Box 3 */}
            <div className="bg-[#09090b]/45 border border-zinc-800/80 rounded-lg p-3 space-y-1">
              <span className="text-xs font-bold text-blue-400 font-mono">training/train.py</span>
              <p className="text-[10px] text-zinc-400 leading-relaxed">
                PEFT framework fine-tuning orchestrator incorporating BitsAndBytes compute routines (NF4 4-bit quantizations), LoRA matrix updates, gradient accumulation, and model savers.
              </p>
            </div>

            {/* Box 4 */}
            <div className="bg-[#09090b]/45 border border-zinc-800/80 rounded-lg p-3 space-y-1">
              <span className="text-xs font-bold text-blue-400 font-mono">backend/rag_faiss.py</span>
              <p className="text-[10px] text-zinc-400 leading-relaxed">
                SentenceTransformers pipeline chunking files recursively and building persistent FAISS vector stores to supply contextual embeddings during chats.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
